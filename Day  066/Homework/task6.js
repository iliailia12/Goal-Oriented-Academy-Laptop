function isFirstAndLastCharacterSame(text) {

    if (text.length === 0) return false;

    const firstChar = text.charAt(0); 
    const lastChar = text.charAt(text.length - 1); 

    return firstChar === lastChar; 
}
//ეს ბოლომდე ჩემი დაწერილი არაა  !

